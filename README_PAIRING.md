Quick start (PC)

1. From repo root run: node ./src/server.ts (requires ts-node or compile to JS)
2. Open phone browser or app and follow pairing flow in docs/PAIRING.md

This is a minimal LAN-only pairing prototype and uses file-based storage in .data/ directory.
